import 'package:get/get.dart';

class Dimensions {
  static double screenHeight = Get.context!.height;
  static double screenWidth = Get.context!.width;

  static double pageView = screenHeight / 2.64;
  static double pageTextContainer = screenHeight / 7.3;

  // Dynamic height, padding and margin
  static double height5 = screenHeight / 175.1;
  static double height10 = screenHeight / 87.5;
  static double height15 = screenHeight / 58.4;
  static double height20 = screenHeight / 43.8;

  // Dynamic width, padding and margin
  static double width5 = screenWidth / 82.3;
  static double width10 = screenWidth / 41.1;
  static double width15 = screenWidth / 27.4;
  static double width20 = screenWidth / 20.57;

  // Font sizes
  static double font16 = screenHeight / 54.7;
  static double font20 = screenHeight / 43.8;
  static double font24 = screenHeight / 36.5;

  // Radius sizes
  static double radius10 = screenHeight / 87.5;
  static double radius15 = screenHeight / 58.3;
  static double radius20 = screenHeight / 43.8;
  static double radius30 = screenHeight / 29.2;

  // Icon sizes
  static double iconSize16 = screenHeight / 20.1;
  static double iconSize20 = screenHeight / 50.25;
  static double iconSize24 = screenHeight / 44.7;

  // List view size
  static double listViewImgSize = screenWidth / 3;
  static double listViewTextContSize = screenWidth / 3.6;

  // Popular food
  static double popularFoodSize = screenHeight / 2.3;

  // Bottom height
  static double bottomHeightBart = screenHeight / 6.75;

  // splash screen dimensions
  static double splashlogoHeight = screenHeight / 2.8;
  static double splashlogoPicHeight = screenHeight / 2.5;
  static double splashlogo2Height = screenHeight / 2.9;
  static double splashlogo2PicHeight = screenHeight / 11.2;
  // static double splashImg = screenHeight /
}
